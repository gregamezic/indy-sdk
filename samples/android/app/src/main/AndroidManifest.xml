<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.hyperledger.indy.sdk">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Example">

        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".ui.anoncreds.AnoncredsActivity" />
        <activity android:name=".ui.anoncreds_revocation.AnoncredsRevocationActivity" />
        <activity android:name=".ui.crypto.CryptoActivity" />
        <activity android:name=".ui.endorser.EndorserActivity" />
        <activity android:name=".ui.ledger.LedgerActivity" />
    </application>

</manifest>